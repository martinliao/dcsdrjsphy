define(['jquery', "core/log", "mod_fullcalendar/js", 'mod_bootstrapbase/bootstrap'], function($, log, cal) {
    debugger;
    //booking.getBookingLists(<?= set_value('seq_no', $form['seq_no']); ?>);
    log.debug('fullcalendar loading...');
    //booking.init();
});